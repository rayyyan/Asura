import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: -22,
  },
  innerContainer: {
    height: 5,
    width: 22,
    backgroundColor: "#fff",
    borderRadius: 5,
    alignSelf: "center",
  },
  unknown: {
    position: "absolute",
    width: "100%",
    zIndex: 1,
    top: 15,
  },
})
